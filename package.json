{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "cd superviser-frontend && npm install && cd ../applicant-frontend && npm install && cd ../backend && npm install",
    "build": "cd superviser-frontend && npm run build && cd ../applicant-frontend && npm run build && cd ../backend"
  }
}
